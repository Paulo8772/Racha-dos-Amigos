// ============================================================
// RACHA DOS AMIGOS — config.example.js
// ============================================================
// 1. Copie este arquivo e renomeie a cópia para "config.js"
// 2. Preencha os dois valores abaixo com os dados do SEU
//    projeto Supabase (Configurações > API no painel).
// 3. NUNCA apague o "config.example.js" original — ele fica
//    no projeto só como modelo, sem dados reais.
// 4. O arquivo "config.js" (com os dados reais) NÃO deve ser
//    enviado para um repositório público — veja o .gitignore
//    quando chegarmos na etapa de publicação.
//
// IMPORTANTE: a "anon key" abaixo é uma chave PÚBLICA — ela é
// feita para ser usada no navegador. A que nunca deve aparecer
// aqui é a "service_role key" (essa fica só no painel do Supabase).
// ============================================================

const SUPABASE_URL = 'https://SEU-PROJETO.supabase.co';
const SUPABASE_ANON_KEY = 'sua-chave-publica-anon-aqui';